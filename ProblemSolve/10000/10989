#include<iostream>
using namespace std;

int arr[10'005];

int main() {
	ios::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);
	int N; cin >> N;
	for (int n = 0; n < N; n++) {
		int tmp; cin >> tmp;
		arr[tmp]++;
	}
	for (int i = 1; i < 10'001; i++) {
		for (int c = 0; c < arr[i]; c++) {
			cout << i << "\n";
		}
	}
}
